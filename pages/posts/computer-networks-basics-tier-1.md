---
title: 计算机网络基础知识 - 第一梯队（极高频）
date: 2025-12-30
categories:
  - 啃啃原理
tags:
  - 计算机网络
  - 基础
  - 极高频
---

## 1. GET和POST的请求的区别

**GET**：幂等且安全

- 用于从服务器获取数据。
- 参数附加在URL中，数据长度有限（URL长度限制）。
- 不会更改服务器上的数据。
- 更快，因其数据嵌入在URL中，适用于查询操作。
- 数据安全性较低，因为参数可以被暴露在URL中。
- 对浏览器缓存友好。

**POST**：非幂等且不安全

- 用于向服务器发送数据，通常会更改服务器上的数据（如表单提交）。
- 参数通过HTTP请求体传输，没有数据长度限制。
- 安全性较高（参数不会暴露在URL中）。
- 不会被浏览器缓存。
- 相对GET来说，处理速度较慢，因为涉及数据的传输。

> 安全性误区纠正：POST 并不比 GET 更安全。
> 真相：在网络传输中，GET 和 POST 都是明文传输。如果你不用 HTTPS，黑客抓包都能看得一清二楚。
> 所谓的“安全”：仅仅是因为 GET 的参数会留在浏览器历史记录、服务器日志里，容易被旁边的人看到，而 POST 的数据在 Body 里，相对隐蔽一点点。

---

## 2.
